/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.giphy.com",
      "nitesh-blog-app.s3.amazonaws.com",
      "images.clerk.dev",
    ],
  },
};

export default nextConfig;
